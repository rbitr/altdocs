# Task 019: Text Color and Highlight Color

## Priority
P2 — Extended Formatting

## Description

Add text color and highlight (background) color support.

### Requirements
- Add `color` and `backgroundColor` properties to `TextStyle` interface
- Implement color picker UI in toolbar (can be a simple grid of preset colors)
- Renderer should apply `color` and `background-color` CSS to styled spans
- `stylesEqual` function must account for new color properties
- Add unit tests for model changes
- Add e2e tests for color application

## Dependencies
None
